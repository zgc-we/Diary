文件切片上传：
处理较大文件时，假如我们一个接口上传，我们也许定义的前后端超时时间不够上传文件的事件，通常会返回上传失败。

文件上传两种场景：
1、后台提供静态文件路径，我们直接上传到静态文件目录下，文件进行处理。
2、直接前端转化为 base 64 文件，我们直接上传二进制流。

使用方法： new FileReader()， 此方法中提供给我们转化成base 64的文件格式的流方法。当我们获取到文件的file.size() 时，我们可以切割
现有的文件已多少M为单位，我们切割之后，我们以这种异步方式上传，我们的事件会瞬间，但是后台也要做相应处理。